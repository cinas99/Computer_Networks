package pl.poznan.put.client;

public enum Message {
    UNJOIN,
    JOIN,
    TURN_ON,
    LEAVE,
    UNREADY,
    READY,
    START,
    ROOM_EVENT,
    UDP_CONNECT,
    TRUE,
    FALSE
}
